# MyFirst-ProjectIn-Kotlin

Add dependency to automatic find the id(val edt = findViewById(R.id.edit))-> this two lines ignored all findViewById
    
    
    apply plugin :"kotlin-android"
    apply plugin :"kotlin-android-extensions"
